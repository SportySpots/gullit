import React from 'react';
import { Flex, Box } from 'rebass';

const Header = () => (
  <Flex>
    <Box
      p={3}
      // width={0.5}
      // color="magenta"
      // bg="black"
    >
      Logo
    </Box>
  </Flex>
);

export default Header;
